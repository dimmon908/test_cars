<% users = User::where(:role_id => Role.select(:id).where(:internal_name => :business).first.id) %>
<div class="col-md-4 innerAll">
  <div class="padding-bottom-none-phone">
    <div class="widget-stats widget-stats-gray widget-stats-5">
      <%= link_to t('admin.links.see_all'), { :controller => 'admin', :action => :business}, { :remote => true, 'data-tab' => :business, :class => 'pull-right'} %>

      <h4><%= t('admin.dashboard.companies') %></h4>
      <p><%= "#{users.count} #{t('general.available')}"%></p>

      <%= link_to t('admin.links.new_business'), '/admin/business#new', { 'data-tab' => :business, :class => 'btn btn-primary'} %>
    </div>
  </div>
</div>